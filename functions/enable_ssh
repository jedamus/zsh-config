enable_ssh() {

  #set -x

  pfad=$HOME/.ssh
  private_key_list=$pfad/private_key_list

  eval "$(ssh-agent -s)"

  if [ -f $private_key_list ]; then
    for private_key in  `cat $private_key_list`; do
      ssh-add $pfad/$private_key
    done
  else
    ssh-add $pfad/id_rsa
  fi
}
