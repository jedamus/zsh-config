export OLDPROMPT=$PROMPT
export OLDRPROMPT=$RPROMT
precmd() {
  prmt=zshprompt.py
  if [[ -x $prmt ]]; then
    PROMPT="${$($prmt left):-$PROMPT}"
    RPROMPT="${$($prmt right --last-exit-status=$?):-$RPROMPT}"
  else
    PROMPT=$OLDPROMPT RPROMPT=$OLDRPROMPT
  fi
}
